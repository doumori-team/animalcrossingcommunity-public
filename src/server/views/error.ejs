<!doctype html>
<html lang="en">
<head>
    <title>Error(s) - Animal Crossing Community</title>
    <meta charset="utf-8" />
    <meta name="robots" content="noindex" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            position: absolute;
            width: 100%;
            height: 100%;
        }

        .Alert {
            margin: 0 .625rem .9375rem;
            padding: .5em;
            border-radius: 1.5em;

            font-size: px2rem(14px);
            line-height: 1.5;
            font-weight: normal;

            border-width: 0.3em 1em;
            border-style: solid;
            box-shadow: 0 0 1px 1px rgba(0, 0, 0, .5);
        }

        .Alert-error {
			border-color: hsl(60, 100%, 75%);
			background-color: lighten(hsl(60, 100%, 75%), 15%);
		}
    </style>
    <link rel="icon" href="https://dts8l1aj0iycv.cloudfront.net/images/layout/favicon/gyroid-default.ico">
</head>
<body>
    <article>
		<% if (errors.length === 1) { %>
			<div class='Alert Alert-error' role='alert' aria-live='polite'>
				<%= errors[0].message %> <a href="https://www.animalcrossingcommunity.com">Continue</a>
			</div>
		<% } else { %>
			<% for (let error of errors) { %>
				<div class='Alert Alert-error' role='alert' aria-live='polite'>
					<%= error.message %> <a href="https://www.animalcrossingcommunity.com">Continue</a>
				</div>
			<% } %>
		<% } %>
    </article>
</body>
</html>